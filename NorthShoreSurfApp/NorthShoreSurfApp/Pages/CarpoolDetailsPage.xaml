<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:NorthShoreSurfApp.ViewModels"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:NorthShoreSurfApp"
             x:Class="NorthShoreSurfApp.CarpoolDetailsPage"
             BackgroundColor="White"
             >

    <ContentPage.BindingContext>
        <models:CarpoolDetailsPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid RowSpacing="0"
              Margin="0"
              Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Top navigation bar-->
            <local:CustomNavigationBar x:Name="navigationBar"
                                       BarBackgroundColor="{StaticResource BarBackground}"
                                       ShowBackButton="True"
                                       Title="{local:Translate ride}"
                                       TitleColor="Black"
                                       TitleSize="24"
                                       ShowLogo="False"
                                       Logo="ic_logo.png"
                                       />
            <!--Grid enter data-->
            <Grid x:Name="gridEnterData" 
                  RowSpacing="0"
                  Margin="0"
                  Padding="0"
                  VerticalOptions="Fill" 
                  Grid.Row="1" 
                  HorizontalOptions="Fill" 
                  IsVisible="True"
                  BackgroundColor="White"
                  >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!--ScrollView-->
                <ScrollView>

                    <StackLayout Padding="10" HorizontalOptions="Fill" VerticalOptions="Start">

                        <Frame
                           BackgroundColor="{StaticResource NSSBlue}" 
                            HeightRequest="50"
                            HorizontalOptions="Fill"
                            HasShadow="True"
                            CornerRadius="10">


                            <Grid
                                RowSpacing="0"
                                Margin="0"
                                Padding="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding GetName}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="0" Margin="0,0,5,0"/>
                                <Label Text="{Binding PhoneNo}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="0" VerticalOptions="Center"/>
                                <Label Text="{Binding Age}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="1" VerticalOptions="Center" HorizontalOptions="EndAndExpand"/>
                                <Label Text="{Binding Gender}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="1" VerticalOptions="Center" HorizontalOptions="EndAndExpand"/>

                            </Grid>

                        </Frame>

                        <Frame
                           BackgroundColor="{StaticResource NSSBlue}" 
                            HeightRequest="90"
                            HorizontalOptions="Fill"
                            HasShadow="True"
                            CornerRadius="10">


                            <Grid
                                
                                RowSpacing="0"
                                Margin="0"
                                Padding="0">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding DepatureTime}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="0" HorizontalOptions="StartAndExpand"/>
                                <Label Text="{Binding DepatureTime}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="0" VerticalOptions="StartAndExpand"/>
                                <Label Text="{Binding Price}" LineHeight="0.8" TextColor="White" Grid.Row="2" Grid.Column="0" VerticalOptions="EndAndExpand"/>
                                <Label Text="{Binding NumberOfSeats}" LineHeight="0.8" TextColor="White" Grid.Row="3" Grid.Column="0" VerticalOptions="EndAndExpand"/>
                                <Label Text="{Binding Address}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="1" VerticalOptions="Center" HorizontalOptions="End"/>
                                <Label Text="{Binding ZipAndCity}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="1" VerticalOptions="Start" HorizontalOptions="EndAndExpand"/>
                                <Label Text="{Binding DestinationAddress}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="1" VerticalOptions="Start" HorizontalOptions="EndAndExpand"/>
                                <Label Text="{Binding ZipAndCityDestination }" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="1" VerticalOptions="Start" HorizontalOptions="EndAndExpand"/>

                                <local:CustomImageButton x:Name="btnEdit1"
                                     Grid.Row="0"
                                     Grid.Column="2"
                                     Icon="ic_location.png"
                                     VerticalOptions="Center"
                                     HorizontalOptions="EndAndExpand"
                                     HeightRequest="70"
                                     WidthRequest="70"
                                     Background="{StaticResource NSSBlue}"
                                     BackgroundColor="Transparent"
                                     BackgroundPressed="{StaticResource NSSBluePressed}"
                                     BorderColor="Transparent"
                                     BorderThickness="1"
                                     IconPadding="1"
                                     />
                                
                                <local:CustomImageButton x:Name="btnEdit"
                                     Grid.Row="3"
                                     Grid.Column="2"
                                     Icon="ic_destination.png"
                                     VerticalOptions="StartAndExpand"
                                     HorizontalOptions="EndAndExpand"
                                     HeightRequest="70"
                                     WidthRequest="70"
                                     Background="{StaticResource NSSBlue}"
                                     BackgroundColor="Transparent"
                                     BackgroundPressed="{StaticResource NSSBluePressed}"
                                     BorderColor="Transparent"
                                     BorderThickness="1"
                                     IconPadding="1"
                                     />

                            </Grid>

                        </Frame>

                        <local:CustomImageTextButton
                            Margin="0,10,0,10"
                            Title="{local:Translate join_ride}"                            
                            HorizontalOptions="Fill"
                            CornerRadius="10"
                            Background="Orange"
                            TitleColor="White">
                            
                        </local:CustomImageTextButton>

                    </StackLayout>

                </ScrollView>

            </Grid>
            
        </Grid>
    </ContentPage.Content>
</ContentPage>