<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:NorthShoreSurfApp.ViewModels"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:NorthShoreSurfApp"
             x:Class="NorthShoreSurfApp.CarpoolDetailsPage"
             BackgroundColor="White"
             >

    <ContentPage.BindingContext>
        <models:CarpoolDetailsPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid RowSpacing="0"
              Margin="0"
              Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Top navigation bar-->
            <local:CustomNavigationBar x:Name="navigationBar"
                                       BarBackgroundColor="{StaticResource BarBackground}"
                                       ShowBackButton="True"
                                       Title="{local:Translate ride}"
                                       TitleColor="Black"
                                       TitleSize="24"
                                       ShowLogo="False"
                                       Logo="ic_logo.png"
                                       />
            <!--Grid enter data-->
            <Grid x:Name="gridEnterData" 
                  VerticalOptions="Fill" 
                  Grid.Row="1" 
                  HorizontalOptions="Fill" 
                  IsVisible="True"
                  BackgroundColor="White"
                  >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!--ScrollView-->
                <ScrollView>

                    <StackLayout Padding="10" HorizontalOptions="Fill" VerticalOptions="Start">

                        <Frame
                           BackgroundColor="{StaticResource NSSBlue}" 
                            HeightRequest="60"
                            HorizontalOptions="Fill"
                            HasShadow="True"
                            CornerRadius="10">


                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding GetName}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="0" Margin="0,0,5,0"/>
                                <Label Text="{Binding PhoneNo}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="0" VerticalOptions="Center"/>
                                <Label Text="{Binding Age}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="1" VerticalOptions="Center"/>
                                <Label Text="{Binding Gender}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="1" VerticalOptions="Center"/>

                            </Grid>

                        </Frame>

                        <Frame
                           BackgroundColor="{StaticResource NSSBlue}" 
                            HeightRequest="100"
                            HorizontalOptions="Fill"
                            HasShadow="True"
                            CornerRadius="10">


                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Text="{Binding Address}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="0" Margin="0,0,5,0"/>
                                <Label Text="{Binding PhoneNo}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="0" VerticalOptions="Center"/>
                                <Label Text="{Binding Age}" LineHeight="0.8" TextColor="White" Grid.Row="0" Grid.Column="1" VerticalOptions="Center"/>
                                <Label Text="{Binding Gender}" LineHeight="0.8" TextColor="White" Grid.Row="1" Grid.Column="1" VerticalOptions="Center"/>

                                <local:CustomFrame
                                    IsVisible="True"
                                    Grid.Column="2"
                                    HeightRequest="100"
                                    WidthRequest="20"
                                    
                                    >

                                    <local:CustomImageButton
                                        Icon="ic_destination.png"
                                        >
                                    </local:CustomImageButton>
    
            
                                    
                                </local:CustomFrame>
                                    
                            </Grid>

                        </Frame>

                        <local:CustomImageTextButton
                            Margin="0,10,0,10"
                            Title="{local:Translate join_ride}"                            
                            HorizontalOptions="Fill"
                            CornerRadius="10"
                            Background="Orange"
                            TitleColor="White">
                            
                        </local:CustomImageTextButton>

                    </StackLayout>

                </ScrollView>

            </Grid>
            
        </Grid>
    </ContentPage.Content>
</ContentPage>